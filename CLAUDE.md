# Project notes for Claude

Frontend-only prototype (Vite + React + React Router + Tailwind). Mock data
lives in `src/lib/mockData.js`; shared state in `src/lib/AppContext.jsx`.
Do not add a real backend, real payments, or real auth — this is a UX/flow
prototype only, matching the original Base44 build brief.
